---
- name: Gather and list Amazon S3 buckets
  hosts: localhost
  gather_facts: false
 
  tasks:
    - name: Fetch S3 bucket information
      amazon.aws.s3_bucket_info:
      register: s3_info
 
    - name: Output all S3 bucket names
      ansible.builtin.debug:
        msg: "{{ s3_info.buckets | map(attribute='name') | list }}"
